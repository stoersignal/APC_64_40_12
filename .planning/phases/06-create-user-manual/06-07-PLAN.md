---
phase: 06-create-user-manual
plan: 07
type: execute
wave: 2
depends_on: []
files_modified:
  - docs/INSTALL.md
  - docs/TROUBLESHOOTING.md
autonomous: true
requirements: [DOC-01, DOC-08]
must_haves:
  truths:
    - "docs/INSTALL.md exists and walks a Windows + macOS user from download to verified handshake without referencing source code (DOC-01)"
    - "docs/INSTALL.md preserves the verbatim strings from current README install paragraph: c:\\\\programdata\\\\Ableton\\\\Live 11 Suite\\\\Resources\\\\MIDI Remote Scripts (Windows path), Contents/App-Resources/MIDI Remote Scripts (macOS path), Show Package Contents (macOS Finder verb), MIDI / Sync (Live preferences tab), the -MAIN suffix gotcha"
    - "docs/INSTALL.md confirms Live 11 + Live 12 compatibility, citing __init__.py:31 vendor_id=2536, product_id=115 and APC.py:127-148 Live-12-aware version fallback"
    - "docs/INSTALL.md describes the handshake-verification gesture: hold Shift + press Track Selection — if the clip grid changes layout, install is correct (existing README phrasing, source-traced to APC.py:94-100 _on_handshake_successful)"
    - "docs/TROUBLESHOOTING.md covers all 5 entries from D-11: handshake failures, exclusive-solo bypass, fixed 400ms threshold, no per-track Send buttons, no Activator/arm — each in 'What you might see / Why it happens / What to do' structure (D-12)"
    - "Each TROUBLESHOOTING.md entry is source-traced per PATTERNS section 5b (specific file:line citations)"
    - "No occurrence of the word 'snapshot' in any of docs/INSTALL.md, docs/TROUBLESHOOTING.md (terminology gate from D-09 / D-10)"
  artifacts:
    - path: "docs/INSTALL.md"
      provides: "Windows + macOS install walkthrough with verbatim path strings, handshake verification, version compatibility"
      contains: "Show Package Contents"
    - path: "docs/TROUBLESHOOTING.md"
      provides: "5 failure-mode entries, each with What/Why/What-to-do structure and source-line citations"
      contains: "LONG_PRESS_DELAY"
  key_links:
    - from: "docs/TROUBLESHOOTING.md handshake entry"
      to: "APC.py handshake protocol"
      via: "Citation: APC.py:71-100 + APC.py:127-148 + __init__.py:30-32"
      pattern: "APC\\.py:"
---

<objective>
Write the two prose deliverables that are independent of the HTML/SVG work: `docs/INSTALL.md` (DOC-01) and `docs/TROUBLESHOOTING.md` (DOC-08). Per the I-05 fix, these are independent of Plans 02–06 — they only read source files (`APC.py`, `__init__.py`, `ToggleMomentaryChannelStripComponent.py`, `EncModeSelectorComponent.py`, `ShiftableSelectorComponent.py`) which exist at start. So this plan can run in **wave 2** in parallel with Plan 02 (HTML layout) and Plan 06 (SVG generator), shaving one wave off the phase critical path.

INSTALL.md migrates the existing README install paragraph and expands per D-13. TROUBLESHOOTING.md synthesizes from D-11 / D-12 entry template and PATTERNS section 5b's source-traceable failure modes.

Both artifacts use Ableton-canonical "variations" terminology (D-09 / D-10) — although INSTALL.md and TROUBLESHOOTING.md don't actually need to mention variations (their scope is install + failure modes, not feature documentation). The terminology gate primarily blocks "snapshot" from appearing.

The README rewrite (which depends on `docs/manual.html` and `docs/apc40-layout.svg` existing) is split out to Plan 08.

Output: A new APC40 user can read INSTALL.md and reach a working installed-and-verified state on Windows or macOS. If they hit a problem, TROUBLESHOOTING.md tells them what's happening and what to do.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-create-user-manual/06-CONTEXT.md
@.planning/phases/06-create-user-manual/06-PATTERNS.md
@README.md
@APC.py
@__init__.py
@ToggleMomentaryChannelStripComponent.py
@EncModeSelectorComponent.py
@ShiftableSelectorComponent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/INSTALL.md and docs/TROUBLESHOOTING.md</name>
  <files>docs/INSTALL.md, docs/TROUBLESHOOTING.md</files>
  <read_first>
    - README.md (full file — for the verbatim install paragraph strings to preserve)
    - APC.py:7-9 (MANUFACTURER_ID, ABLETON_MODE constants)
    - APC.py:71-100 (handle_sysex / _on_identity_response / _on_dongle_response / _on_handshake_successful)
    - APC.py:127-148 (_send_introduction_message — Live 12 fallback)
    - __init__.py:24-32 (create_instance + get_capabilities — vendor_id 2536, product_id 115, MIDI port props)
    - ToggleMomentaryChannelStripComponent.py:8 (LONG_PRESS_DELAY = 4)
    - ToggleMomentaryChannelStripComponent.py:33 (setattr(self._track, track_attr, not current) — exclusive-solo bypass evidence)
    - EncModeSelectorComponent.py:9-10 (LONG_PRESS_DELAY = 4 same constant)
    - EncModeSelectorComponent.py:80-81 (number_of_modes() == 4 — no per-track Send buttons)
    - ShiftableSelectorComponent.py:101-102, 112-113 (arm-button wiring — no Activator/arm-record mapping per D-11)
    - .planning/phases/06-create-user-manual/06-PATTERNS.md sections 4 (INSTALL.md analog + verbatim strings) and 5 (TROUBLESHOOTING.md template + 5 source-traced entries)
    - .planning/phases/06-create-user-manual/06-CONTEXT.md decisions D-09, D-10 (no snapshot terminology), D-11 (failure mode list), D-12 (entry structure), D-13 (Windows + macOS only, sections list), D-14 (screenshots not required)
  </read_first>
  <action>
    **A. Write `docs/INSTALL.md`.** Use Write tool to create file. Structure:

    ```markdown
    # APC_64_40_11 — Install & First-Run Setup

    This guide walks you from a fresh clone or ZIP download to a working APC40 controller in Ableton Live 11 or Live 12. Tested on Windows and macOS. Linux is not officially supported (Ableton Live does not ship for Linux).

    ## What you need

    - An Akai APC40 (vendor ID 2536, product ID 115)
    - Ableton Live 11 or Live 12 installed
    - The `APC_64_40_11` folder from this repo (download as ZIP from GitHub, or `git clone`)

    ## Step 1 — Place the script in Ableton's MIDI Remote Scripts directory

    The script must live in Ableton's `MIDI Remote Scripts` folder. The folder is in different places on Windows and macOS:

    ### Windows

    Default location:
    ```
    c:\programdata\Ableton\Live 11 Suite\Resources\MIDI Remote Scripts
    ```
    (Replace `Live 11 Suite` with your installed edition, e.g. `Live 12 Standard`. The path always ends in `Resources\MIDI Remote Scripts`.)

    After placing the folder, you should see all this repo's `.py` files inside:
    ```
    c:\programdata\Ableton\Live 11 Suite\Resources\MIDI Remote Scripts\APC_64_40_11
    ```

    **GitHub ZIP gotcha:** if you downloaded a ZIP from GitHub, the extracted folder may be named `APC_64_40_11-MAIN` or `APC_64_40_12-main`. Rename it to `APC_64_40_11` (drop the `-MAIN` / `-main` suffix) — Live ignores folders with non-standard names.

    ### macOS

    1. In Finder, locate the `Ableton Live 11 Suite.app` (or `Ableton Live 12 Suite.app`) bundle.
    2. Right-click → **Show Package Contents**.
    3. Navigate into:
       ```
       Contents/App-Resources/MIDI Remote Scripts
       ```
    4. Drop the `APC_64_40_11` folder here.

    ## Step 2 — Select the script in Live's preferences

    1. Launch Ableton Live.
    2. Open `Live → Preferences` (or `Ableton Live → Settings` on macOS).
    3. Switch to the **MIDI / Sync** tab.
    4. In the **Control Surface** column, click the dropdown for the row matching your APC40 and select **APC_64_40_11**.
    5. In the **Input** and **Output** columns, set both to your APC40's port.
    6. Make sure the **Track**, **Sync**, and **Remote** toggle checkboxes match the standard pattern (Track and Remote on for both input and output; the script declares this via `__init__.py` `get_capabilities()`).

    ## Step 3 — Verify the handshake

    With the APC40 connected and the script selected, the controller initialization performs an identity + dongle handshake (see `APC.py:71–100` for the sequence). Confirm the handshake worked using the existing gesture:

    > Hold the **Shift** button on the APC40 and press the various **Track Selection** buttons to switch matrix modes. If the clip grid changes layout (e.g. flips into Note Mode 1, Session Overview, etc.), the script is loaded and talking to the controller.

    The script supports both Live 11 and Live 12. Live 12 changed the introduction-message API; the script handles the fallback automatically (`APC.py:127–148` `_send_introduction_message`).

    ## Next

    - [Interactive controller manual](manual.html) — open in any browser to learn every control.
    - [Troubleshooting](TROUBLESHOOTING.md) — if the handshake fails or behavior surprises you.
    ```

    **B. Write `docs/TROUBLESHOOTING.md`.** Use Write tool to create file. Structure (5 entries, each in What/Why/What-to-do format per D-12):

    ```markdown
    # APC_64_40_11 — Troubleshooting & Known Limitations

    This document covers the failure modes and intentional limitations a user is likely to encounter. Every entry is traced to the source file and line range that implements (or omits) the behavior.

    ## 1. The script doesn't appear in Live's MIDI preferences (handshake failure)

    **What you might see:** You select `APC_64_40_11` in Live's Control Surface dropdown, but the APC40's clip grid stays dark, or you can't get any feedback from the controller. Or the dropdown doesn't list `APC_64_40_11` at all.

    **Why it happens:** Live discovers control surface scripts by scanning the `MIDI Remote Scripts` folder at startup and matching against `get_capabilities()` in `__init__.py:30–32` (which declares vendor ID 2536 and product ID 115 for Akai APC40). If the folder is misnamed (e.g. `APC_64_40_11-MAIN` from a GitHub ZIP), Live skips it. If the script loads but the handshake doesn't complete, the LED feedback never enables — `APC.py:94–100` `_on_handshake_successful` is what enables all components.

    **What to do:**
    1. Confirm the folder name is exactly `APC_64_40_11` (no `-MAIN` / `-main` suffix).
    2. Confirm you're in the right `Resources/MIDI Remote Scripts` folder for your installed Live edition (Suite vs Standard, Live 11 vs Live 12).
    3. Restart Ableton Live after dropping the folder — Live only scans for scripts at startup.
    4. In Live's MIDI preferences, set both Input and Output to the APC40 ports.
    5. Disconnect / reconnect the APC40 USB cable and select the script again.
    6. If you're on Live 12 and the script seems to load but never lights up, this script handles Live 12's API change (`APC.py:127–148`) — confirm you have the latest version of this repo.

    ## 2. Pressing Solo on track A doesn't un-solo track B (exclusive-solo bypass)

    **What you might see:** Live's mixer normally has "exclusive solo" — soloing one track un-solos all others. With this script, pressing Solo on the APC40 leaves other soloed tracks soloed too.

    **Why it happens:** This is intentional. The script's Solo state machine writes `track.solo` directly via `setattr(self._track, 'solo', not current)` (`ToggleMomentaryChannelStripComponent.py:33`). It does NOT call Live's exclusive-solo coordinator, because the toggle/momentary unified model (DOC-03) needs deterministic per-track state — exclusive solo would interfere with the momentary "release reverts" semantics.

    **What to do:**
    - To un-solo all tracks: click solo on each soloed track to toggle it off, or use Live's mixer (mouse) which goes through the exclusive coordinator.
    - This is by design and not user-configurable.

    ## 3. The 400ms long-press threshold feels wrong (not adjustable)

    **What you might see:** Some users want a shorter (e.g. 250ms) momentary threshold. Some want longer (600ms+).

    **Why it happens:** The threshold is hardcoded in two places: `ToggleMomentaryChannelStripComponent.py:8` (`LONG_PRESS_DELAY = 4`) for Solo / Mute, and `EncModeSelectorComponent.py:9–10` (also `LONG_PRESS_DELAY = 4`) for Send A / B / C mode buttons. The constant is in 100ms ticks (Ableton's MIDI timer fires every 100ms), so 4 ticks × 100ms = 400ms.

    **What to do:**
    - The threshold is not exposed as a user-configurable setting. Adjusting it requires editing the source code.
    - The 400ms value was chosen to be long enough that a deliberate hold feels distinct from an accidental long press, while short enough that holding doesn't feel laggy. If you need to change it, edit both files (Solo/Mute and Send mode use the same threshold by design).

    ## 4. There are no per-track Send A / B / C buttons

    **What you might see:** You're looking for a button that lets you adjust Track 5's Send B without switching to Send B mode globally first.

    **Why it happens:** The APC40 hardware has only global Send-mode selectors (the Pan / Send A / B / C row), not per-track Send buttons. The script reflects the hardware: `EncModeSelectorComponent.py:80–81` declares `number_of_modes() == 4` (Pan / Send A / Send B / Send C are the only encoder modes the script wires).

    **What to do:**
    - To adjust Track 5's Send B, press Send B (the encoder mode button), then turn Track 5's top encoder. The Send A/B/C buttons themselves use the toggle/momentary unified model — short tap latches; hold ≥400ms for a temporary jump that reverts to Pan on release.

    ## 5. There are no Track Activator (arm-record) buttons

    **What you might see:** You expect the row of buttons under each track to include Track Activator / arm-record (the way some other APC40 scripts wire them).

    **Why it happens:** Deliberate scope. The script wires the per-track button rows for Solo and Mute only (the toggle/momentary model — DOC-03). Arm-record is wired only conditionally inside the channel-strip when not in step-sequencer mode and not in shift mode (`ShiftableSelectorComponent.py:101–102, 112–113`). There is no momentary/Activator-style mapping.

    **What to do:**
    - To arm a track for recording, use Live's mixer (mouse), or use a different control surface script. This script is intentionally focused on the Solo / Mute / Pan / Send / variation use cases.

    ## Notes

    - "Variations" in this script means Ableton's canonical Macro Rack variations. Use this term when searching Ableton's documentation or forums.
    - Source files referenced in this document are at the repo root (e.g. `APC.py`, `__init__.py`, `ToggleMomentaryChannelStripComponent.py`, `EncModeSelectorComponent.py`, `ShiftableSelectorComponent.py`). They are read-only references from a user's perspective; modifying them is out of scope for end-user use.
    ```

    **Important:** The "Notes" paragraph must NOT mention "snapshot". Per D-09 / D-10, the user-facing terminology gate forbids "snapshot" in new doc artifacts. The internal git history retains "snapshot" but new docs do not surface it.

    Final terminology gate check: `grep -ci 'snapshot' docs/INSTALL.md` and `grep -ci 'snapshot' docs/TROUBLESHOOTING.md` must both return 0.
  </action>
  <verify>
    <automated>test -f docs/INSTALL.md && test -f docs/TROUBLESHOOTING.md && grep -q 'Show Package Contents' docs/INSTALL.md && grep -q 'c:\\\\programdata\\\\Ableton\\\\Live' docs/INSTALL.md && grep -q 'Contents/App-Resources/MIDI Remote Scripts' docs/INSTALL.md && grep -q 'MIDI / Sync' docs/INSTALL.md && grep -q '\-MAIN' docs/INSTALL.md && grep -q 'vendor' docs/INSTALL.md && grep -q 'Live 11' docs/INSTALL.md && grep -q 'Live 12' docs/INSTALL.md && grep -q 'APC.py:' docs/INSTALL.md && grep -q 'LONG_PRESS_DELAY' docs/TROUBLESHOOTING.md && grep -q '400ms\|400 ms' docs/TROUBLESHOOTING.md && grep -q 'ToggleMomentaryChannelStripComponent.py' docs/TROUBLESHOOTING.md && grep -q 'EncModeSelectorComponent.py' docs/TROUBLESHOOTING.md && grep -q 'ShiftableSelectorComponent.py' docs/TROUBLESHOOTING.md && grep -q 'APC.py' docs/TROUBLESHOOTING.md && grep -q '__init__.py' docs/TROUBLESHOOTING.md && grep -q 'What you might see' docs/TROUBLESHOOTING.md && grep -q 'Why it happens' docs/TROUBLESHOOTING.md && grep -q 'What to do' docs/TROUBLESHOOTING.md && [ "$(grep -c 'What you might see' docs/TROUBLESHOOTING.md)" = "5" ] && [ "$(grep -ci 'snapshot' docs/INSTALL.md)" = "0" ] && [ "$(grep -ci 'snapshot' docs/TROUBLESHOOTING.md)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `docs/INSTALL.md` exists and contains:
      - The verbatim Windows path: `c:\programdata\Ableton\Live 11 Suite\Resources\MIDI Remote Scripts`
      - The verbatim macOS gesture: `Show Package Contents`
      - The verbatim macOS path: `Contents/App-Resources/MIDI Remote Scripts`
      - The Live preferences tab name: `MIDI / Sync`
      - The `-MAIN` suffix gotcha
      - The handshake-verification gesture (Shift + Track Selection)
      - Version compatibility (`Live 11` and `Live 12`)
      - At least one source-line citation (e.g. `APC.py:127–148` or `__init__.py`)
    - `docs/TROUBLESHOOTING.md` exists and:
      - Contains exactly 5 entries (verifiable: `grep -c 'What you might see' docs/TROUBLESHOOTING.md` returns 5).
      - Each entry has the three sub-headings: `What you might see`, `Why it happens`, `What to do` (D-12 template).
      - Cites all 5 source files: `APC.py`, `__init__.py`, `ToggleMomentaryChannelStripComponent.py`, `EncModeSelectorComponent.py`, `ShiftableSelectorComponent.py`.
      - Contains the constant `LONG_PRESS_DELAY` and the value `400ms` (or `400 ms`).
    - Case-insensitive `snapshot` count is 0 in BOTH files.
  </acceptance_criteria>
  <done>
    A new APC40 user can read INSTALL.md and reach a working installation on Windows or macOS without asking questions. If they encounter exclusive-solo bypass / 400ms threshold / no-arm-buttons / handshake failures, TROUBLESHOOTING.md tells them what's happening and what to do, with source citations they can verify if curious. No snapshot terminology in either file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader → Markdown files | Both files are read-only static prose. No code execution, no input handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | N/A | docs/INSTALL.md, docs/TROUBLESHOOTING.md | accept | Documentation-only — same justification as the other plans. The only "executable" instructions are paths users navigate in their OS file manager and prefs settings they click in Live; nothing is run automatically. |
</threat_model>

<verification>
Manual review pass:
1. Open `docs/INSTALL.md` in a Markdown viewer / GitHub preview — read it as if you've never installed an Ableton script before. Verify every step is unambiguous and the path strings are correct for both OSes.
2. Open `docs/TROUBLESHOOTING.md` — read through the 5 entries and confirm each follows What/Why/What-to-do.
3. Run `grep -ci snapshot docs/INSTALL.md docs/TROUBLESHOOTING.md` — must return 0 for both.
</verification>

<success_criteria>
DOC-01 (install + first-run setup) and DOC-08 (troubleshooting + known limitations) are fully covered. Terminology gate (no `snapshot` in any new prose) passes for both files. Plan runs in wave 2 in parallel with Plan 02 (HTML layout) and Plan 06 (SVG generator) — saves one wave on the phase critical path per the I-05 fix.
</success_criteria>

<output>
After completion, create `.planning/phases/06-create-user-manual/06-07-SUMMARY.md` documenting:
- The line counts of INSTALL.md, TROUBLESHOOTING.md.
- The five TROUBLESHOOTING.md entry titles and their source-file citations.
- Confirmation that both files pass the snapshot=0 terminology gate.
- Note: Plan 08 (separate) handles the README rewrite, which links to the artifacts created here plus docs/manual.html (Plan 01) and docs/apc40-layout.svg (Plan 06).
</output>
</task>
