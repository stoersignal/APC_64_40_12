---
phase: 06-create-user-manual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/manual.html
autonomous: true
requirements: [DOC-02]
must_haves:
  truths:
    - "docs/manual.html opens in a browser and renders the page shell with header, dark theme, and an empty <div class='device'> container"
    - "Hovering a placeholder pad shows a floating tooltip after a 350ms delay (350ms hover delay verbatim from example)"
    - "All 16 CSS color tokens from example/lp-interactive-map.html lines 8–25 are present, with --lp-case renamed to --apc-case and --lp-border renamed to --apc-border (other 14 tokens unchanged)"
    - "addTooltipEvents(), showTooltip(), hideTooltip() are defined exactly as in example lines 401–451 (350ms timeout + edge-flip + contentGetter indirection)"
    - "Three placeholder mode-strip containers (#encoder-mode-strip, #matrix-mode-strip, #modifier-overlay-strip) exist as empty <div>s — no buttons yet"
    - "No occurrence of the word 'snapshot' anywhere in docs/manual.html"
  artifacts:
    - path: "docs/manual.html"
      provides: "Page skeleton, inline CSS palette (renamed for APC40), tooltip JS, three empty mode-strip containers"
      contains: "function showTooltip("
  key_links:
    - from: "docs/manual.html (placeholder pad elements)"
      to: "addTooltipEvents() / showTooltip()"
      via: "mouseenter listener firing tooltip after 350ms delay"
      pattern: "addTooltipEvents\\(.*=>"
---

<objective>
Create the foundation file `docs/manual.html` — a single self-contained HTML file with inline CSS palette (APC40-renamed), inline JS for tooltips (verbatim port from example), header + footer prose, an empty `.device` container, and three empty mode-strip stubs. No layout content yet, no mode logic yet — this plan is the skeleton that Plans 02–05 build into.

Purpose: Establish the inline-only deliverable foundation per D-01 / D-08. Lock in the dark-theme palette (D-07), the tooltip pattern (D-06), and the three-strip architecture (D-04a) before any APC40-specific layout work begins.

Output: `docs/manual.html` that loads cleanly in a browser, shows the page header, has a dark background, and a single placeholder pad whose hover triggers a working tooltip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-create-user-manual/06-CONTEXT.md
@.planning/phases/06-create-user-manual/06-PATTERNS.md
@example/lp-interactive-map.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/manual.html shell with renamed CSS palette and inline tooltip JS</name>
  <files>docs/manual.html</files>
  <read_first>
    - example/lp-interactive-map.html lines 1–195 (CSS palette + page header + body styles + pad styles + tooltip CSS) — port verbatim with two renames
    - example/lp-interactive-map.html lines 401–451 (showTooltip / hideTooltip / addTooltipEvents — port verbatim, no changes)
    - .planning/phases/06-create-user-manual/06-PATTERNS.md sections 1a, 1c, 1d, 1e (the exact line ranges and verbatim CSS/JS to port)
    - .planning/phases/06-create-user-manual/06-CONTEXT.md decisions D-01, D-04a, D-06, D-07, D-08, D-09 (locked decisions on file layout, three-strip architecture, tooltip pattern, theme, inline-only, terminology)
  </read_first>
  <action>
    Create the file `docs/manual.html` (the directory `docs/` does not exist yet — create it via the Write tool, which auto-creates parent dirs).

    Structure (in order):

    1. `<!DOCTYPE html>` + `<html lang="en">` + `<head>` with:
       - `<meta charset="UTF-8">` and `<meta name="viewport" ...>`
       - `<title>APC_64_40_11 — Interactive Controller Manual</title>`
       - A single `<style>` block containing the full inline CSS (no external CSS files).

    2. Inline CSS — port verbatim from example/lp-interactive-map.html lines 7–195 with these EXACT changes:
       - In the `:root` block, rename ONLY these two tokens:
         - `--lp-case: #1e293b;` → `--apc-case: #1e293b;`
         - `--lp-border: #334155;` → `--apc-border: #334155;`
       - Keep ALL 14 other tokens unchanged: `--bg-color`, `--pad-off`, `--text-main`, `--text-dim`, `--glow-cyan`, `--glow-magenta`, `--glow-blue`, `--glow-green`, `--glow-yellow`, `--glow-red`, `--glow-orange`, `--glow-purple`, `--tooltip-bg`, `--tooltip-border`.
       - Update every `var(--lp-case)` and `var(--lp-border)` reference in the CSS body to use the new `--apc-*` names. Search-and-replace; there are no other lp-* tokens.
       - Keep the rest of the CSS verbatim: `body`, `header`, `header h1` gradient, `.device` (note: `.device` will get its `grid-template-areas` reshaped in Plan 02 — for this plan, port the example's grid CSS as-is so the file is valid; Plan 02 replaces it), `.pad`, `.pad.rect-h`, `.pad.rect-v`, `.pad.circle`, `.pad.active`, `.pad:hover`, `.label`, `.legend-key`, `#tooltip`, `#tooltip.visible`, `#tooltip strong`, `#tooltip .related`.

    3. `<body>` content:
       - `<header>` with `<h1>APC_64_40_11 Interactive Controller Manual</h1>` and a `<p>` subtitle: `Hover any control for behavior. Click a mode button to repaint the relevant region.`
       - A `<div id="legend-desc"></div>` placeholder (Plans 03/04 populate it on mode change).
       - Three empty mode-strip containers, each a sibling `<div>` with id and a comment marker so downstream plans can locate them:
         ```html
         <!-- Encoder mode strip — populated by Plan 03 -->
         <div id="encoder-mode-strip" class="mode-strip"></div>

         <!-- Matrix mode strip — populated by Plan 04 -->
         <div id="matrix-mode-strip" class="mode-strip"></div>

         <!-- Modifier overlay strip — populated by Plan 05 -->
         <div id="modifier-overlay-strip" class="mode-strip"></div>
         ```
       - `<div class="device" id="device"></div>` — empty container; Plan 02 fills it.
       - `<div id="tooltip"></div>` — the floating tooltip target, exactly like example line 199.
       - One temporary placeholder pad to prove tooltips work:
         ```html
         <div id="placeholder-pad" class="pad" style="position:fixed; bottom:20px; left:20px; width:120px;">Hover me</div>
         ```
         (Plan 02 deletes this placeholder.)

    4. Inline JS at the end of `<body>` (single `<script>` block, no external JS):
       - Verbatim port of `let tooltipTimeout;`, `function showTooltip(e, content)`, `function hideTooltip()`, `function addTooltipEvents(element, contentGetter)` from example lines 401–451. Preserve the 350ms timeout, the edge-flip logic (`if (tx + tooltip.offsetWidth > w)`), the `mousemove` handler, and the `mouseleave` cleanup.
       - Add a one-line wiring for the placeholder: `addTooltipEvents(document.getElementById('placeholder-pad'), () => '<strong>Tooltip Test</strong>If you can read this, the tooltip pipeline is alive.');`
       - Define module-level state stubs for downstream plans (initialized but not used yet):
         ```js
         let currentEncoderMode = 'Pan';
         let currentMatrixMode = 'ClipLaunch';
         let modifierOverlays = { ShiftHeld: false, SaveMode: false };
         ```
       - Add a `// PLAN-02 builds the layout below this line` marker comment to anchor downstream plans.

    5. CSS addition (small): add a `.mode-strip` rule for the three strip containers — minimal styling, e.g., `display: flex; gap: 8px; flex-wrap: wrap; justify-content: center; margin: 1rem 0;`. Plans 03/04/05 add buttons inside.

    6. Terminology gate: search the finished file — the substring `snapshot` (case-insensitive) must NOT appear. The word `variations` is allowed but not required at this stage (Plan 05 introduces it via the Tap Tempo / Nudge tooltips).

    Use the Write tool to create the file. Do not use a heredoc.
  </action>
  <verify>
    <automated>test -f docs/manual.html && grep -q '--apc-case' docs/manual.html && grep -q '--apc-border' docs/manual.html && ! grep -q -- '--lp-case' docs/manual.html && ! grep -q -- '--lp-border' docs/manual.html && grep -q 'function showTooltip(' docs/manual.html && grep -q 'function hideTooltip()' docs/manual.html && grep -q 'function addTooltipEvents(' docs/manual.html && grep -q '350' docs/manual.html && grep -q 'id="encoder-mode-strip"' docs/manual.html && grep -q 'id="matrix-mode-strip"' docs/manual.html && grep -q 'id="modifier-overlay-strip"' docs/manual.html && grep -q 'id="tooltip"' docs/manual.html && [ "$(grep -ci 'snapshot' docs/manual.html)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `docs/manual.html` exists.
    - Contains `--apc-case` and `--apc-border` (renamed tokens).
    - Does NOT contain `--lp-case` or `--lp-border` (no leftovers from example).
    - Contains `function showTooltip(`, `function hideTooltip()`, `function addTooltipEvents(`.
    - Contains the literal string `350` (the hover delay millisecond value).
    - Contains `id="encoder-mode-strip"`, `id="matrix-mode-strip"`, `id="modifier-overlay-strip"`.
    - Contains `id="tooltip"` for the floating tooltip target.
    - Contains the marker comment `PLAN-02 builds the layout below this line`.
    - Contains `let currentEncoderMode = 'Pan'`, `let currentMatrixMode = 'ClipLaunch'`, and `modifierOverlays = {`.
    - The 14 unchanged tokens (`--bg-color`, `--pad-off`, `--text-main`, `--text-dim`, `--glow-cyan`, `--glow-magenta`, `--glow-blue`, `--glow-green`, `--glow-yellow`, `--glow-red`, `--glow-orange`, `--glow-purple`, `--tooltip-bg`, `--tooltip-border`) all appear in the file (verifiable: `grep -c '\-\-glow-' docs/manual.html` >= 8).
    - Case-insensitive count of `snapshot` is 0 (terminology gate from D-09 / D-10).
    - File is valid HTML5 — opens in a browser without console errors and the placeholder pad's tooltip appears on hover.
  </acceptance_criteria>
  <done>
    docs/manual.html loads in a browser, shows a header, dark background, and a "Hover me" placeholder in the bottom-left whose tooltip "Tooltip Test / If you can read this, the tooltip pipeline is alive." appears after 350ms of hover. No snapshot terminology. Three empty mode-strip containers and an empty .device container are in the DOM ready for downstream plans.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → static HTML | The user opens docs/manual.html in their browser; the file is read-only static content with no network requests, no cookies, no input handling. No trust boundary crossed at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | N/A | docs/manual.html | accept | Documentation-only — no executable code paths, no auth, no network surface, no untrusted input. The HTML is a static reference document; the only "code" is inline JS that wires tooltips on local DOM elements. No mitigation needed. |
</threat_model>

<verification>
Open docs/manual.html in a modern browser (Chrome / Firefox / Safari). Confirm:
- Page renders with dark background, white header text.
- No JS console errors.
- The "Hover me" placeholder pad in the bottom-left shows a tooltip on hover.
- View source: the three mode-strip `<div>`s exist; the `.device` `<div>` is empty.
</verification>

<success_criteria>
docs/manual.html is a valid, self-contained HTML5 file that loads with no errors, has the APC40-renamed CSS palette, the verbatim tooltip JS, and three empty mode-strip stubs ready for Plans 02–05.
</success_criteria>

<output>
After completion, create `.planning/phases/06-create-user-manual/06-01-SUMMARY.md` documenting:
- The CSS rename: `--lp-case` → `--apc-case`, `--lp-border` → `--apc-border` (and the line count of the file).
- The three mode-strip ids exposed for downstream plans.
- The marker-comment anchor (`PLAN-02 builds the layout below this line`).
- Confirmation that the terminology gate (`snapshot` count = 0) passes.
</output>
