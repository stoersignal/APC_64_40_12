---
status: partial
phase: 06-create-user-manual
source: [06-VERIFICATION.md, user reference image 2026-05-02]
started: 2026-05-02T11:55:00Z
updated: 2026-05-02T11:55:00Z
---

## Current Test

[awaiting layout overhaul gap plan]

## Tests

### 1. docs/manual.html visual layout matches actual APC40 hardware

expected: |
  When opened in a browser, docs/manual.html renders a controller layout
  that matches the APC40's physical region arrangement:
  - Clip Launch grid (8×5) in the TOP-LEFT
  - Scene Launch column (5 pads) immediately right of clip grid
  - Clip Stop row + Stop All Clips below clip grid
  - Track Selection row (8 + Master) below clip-stop
  - Solo / Mute / Activator-Arm rows below track-selection
  - Channel sliders + Master fader bottom-LEFT, with Crossfader between them
  - Top encoders (TRACK CONTROL, 2 rows × 4) top-RIGHT
  - PAN / SEND A / SEND B / SEND C buttons below top encoders
  - SELECT cursor pad + SHIFT right of PAN/Send buttons
  - TAP TEMPO + NUDGE −/+ right of cursor pad
  - DEVICE LOCK ("D") between Nudge+ and Device Control area
  - Device Control encoders (FX region, 2 rows × 4) mid-RIGHT
  - CLIP/TRACK + DEVICE ON/OFF + bank arrows below device encoders
  - DETAIL VIEW / REC QUANTIZATION / MIDI OVERDUB / METRONOME row below
  - PLAY / STOP / REC small cluster bottom-RIGHT
  - Cue Level knob between PADS region and FX region
result: failed

#### Gap detail

The current build (committed by Plan 06-02 and extended by Plans 03-05) has
an essentially-inverted layout:
- Clip grid is in the middle (rows 9-13) instead of top-left (rows 1-5)
- Solo/Mute/Arm are above the clip grid instead of below it
- Top + Device encoders are laid out as 1×8 strips in the LEFT column
  instead of 2×4 grids in the TOP-RIGHT and MID-RIGHT
- Track Selection row, PAN/Send-A/B/C button row, SELECT cursor pad,
  Tap Tempo, Nudge buttons, the green "D" button, the Detail View row,
  and the Cue Level knob are all MISSING from the static HTML
- Stop All Clips and Crossfader exist but at wrong positions

debug_session: (none — straightforward layout correction, no investigation needed)

#### Resolution path

This is a buildLayout overhaul of docs/manual.html — the
`grid-template-areas` declaration (line 94) plus the ~280 lines of static
hardware-region <div> elements need to be rewritten to match the APC40
hardware footprint shown in the user's reference image.

Recommended next step: `/gsd-plan-phase 6 --gaps` to scope the overhaul
as a structured gap-closure plan, then `/gsd-execute-phase 6 --gaps-only`
to apply it.

### 2. docs/apc40-layout.svg matches actual APC40 hardware

expected: |
  The static-poster fallback (apc40-layout.svg) renders the same hardware
  arrangement as docs/manual.html, generated by docs/gen_apc40_layout.py.
result: pending
note: |
  Currently shares the same layout data as manual.html via
  gen_apc40_layout.py. If manual.html's layout is corrected, the SVG
  generator must be regenerated alongside (D-03 commits both .py and .svg).

### 3. INSTALL.md walks a non-developer through Windows + macOS install

expected: |
  Plain-language install steps; verbatim path strings for Live's MIDI
  Remote Scripts directory on both platforms; first-time selection in
  Live's Preferences; handshake verification gesture; Live 11 + Live 12
  compatibility note.
result: pending
note: |
  File written by Plan 06-07 with verified source citations. Pending
  end-user readability check.

### 4. TROUBLESHOOTING.md addresses actual user pain points

expected: |
  Each entry uses What/Why/What-to-do format, cites the originating
  source file, and the entries collectively cover handshake failure,
  exclusive-solo bypass, fixed 400ms threshold, no per-track Send
  buttons, no Activator/Arm.
result: pending
note: |
  Five entries written by Plan 06-07. Pending end-user assessment.

## Summary

total: 4
passed: 0
issues: 1
pending: 3
skipped: 0
blocked: 0

## Gaps

- gap_id: layout-mismatch
  description: docs/manual.html static HTML layout does not match the actual APC40 hardware footprint (per user reference image 2026-05-02)
  severity: high
  scope: docs/manual.html (line 94 grid-template-areas + ~280 lines of hardware-region static <div> elements) + docs/gen_apc40_layout.py + docs/apc40-layout.svg
  open_questions:
    - "What is the green 'D' box between Nudge+ and Device Control? (DEVICE LOCK?)"
    - "What is item '21' in the image? (Crossfader between sliders and master area?)"
  recommended_path: /gsd-plan-phase 6 --gaps
