---
phase: 06-create-user-manual
plan: 08
type: execute
wave: 3
depends_on: [06-01, 06-06]
files_modified:
  - README.md
autonomous: true
requirements: [DOC-02]
must_haves:
  truths:
    - "README.md is rewritten to a thin entry pointer: project blurb + image preserved, three documentation links (INSTALL / manual.html / TROUBLESHOOTING) + the Credits section verbatim from current README.md:32-37"
    - "README.md drops the broken remotescripts.blogspot.com link block and the PDF reference per D-02"
    - "README.md is ≤ 25 lines after rewrite"
    - "All four link targets exist on disk at write time: docs/INSTALL.md (Plan 07), docs/manual.html (Plan 01), docs/TROUBLESHOOTING.md (Plan 07), docs/apc40-layout.svg (Plan 06)"
    - "No occurrence of the word 'snapshot' in README.md (terminology gate from D-09 / D-10)"
  artifacts:
    - path: "README.md"
      provides: "Thin entry pointer — project blurb + 3 doc links + Credits"
      contains: "[Install"
  key_links:
    - from: "README.md"
      to: "docs/INSTALL.md, docs/manual.html, docs/TROUBLESHOOTING.md, docs/apc40-layout.svg"
      via: "Markdown links in the new Documentation section"
      pattern: "\\[.*\\]\\(docs/"
---

<objective>
Rewrite `README.md` to a thin entry pointer per D-02. Per the I-05 fix, this work is split out from the original Plan 07 into its own plan because (a) it has different dependencies (needs `docs/manual.html` from Plan 01 and `docs/apc40-layout.svg` from Plan 06) and (b) splitting compresses the phase critical path from 6 waves to 5 waves.

Plan 08 runs in **wave 3** in parallel with Plan 03 (encoder mode strip) and Plan 04 (matrix mode strip). It depends on Plan 01 (which creates `docs/manual.html` as a skeleton — link target exists from wave 1) and Plan 06 (which creates `docs/apc40-layout.svg` — link target exists from wave 2). It does NOT depend on Plan 07 because the link targets `docs/INSTALL.md` and `docs/TROUBLESHOOTING.md` only need to exist when the README is read by a user — by phase end, both will exist (Plan 07 also runs in wave 2). However, to be safe and avoid race-condition surprises in the executor (which may verify links during write), this plan optionally pre-checks that the four link targets exist before completing — see acceptance_criteria.

The new README links to `docs/INSTALL.md`, `docs/manual.html`, `docs/TROUBLESHOOTING.md`, and `docs/apc40-layout.svg`. The current install paragraph (README.md lines 7-20) was migrated to `docs/INSTALL.md` by Plan 07, so the README's install section is dropped. The "Instructions" section (current lines 22-30) is dropped per D-02 (broken blogspot links, PDF reference, YouTube link).

Output: A user landing on the GitHub repo sees a 15–20 line README with a project blurb, the device image, three documentation links (and the SVG poster link), and the credits section preserved verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-create-user-manual/06-CONTEXT.md
@.planning/phases/06-create-user-manual/06-PATTERNS.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md to thin entry pointer</name>
  <files>README.md</files>
  <read_first>
    - README.md (current 38-line file — preserve the project blurb on line 3, the IMAGE on line 4, the Credits content from lines 32-37 verbatim)
    - .planning/phases/06-create-user-manual/06-PATTERNS.md section 6 (README.md rewrite analog + acceptance criteria including line count constraint)
    - .planning/phases/06-create-user-manual/06-CONTEXT.md decisions D-02 (README becomes thin pointer with three links + credits, drop PDF reference and broken blogspot links)
  </read_first>
  <action>
    Replace the entire content of `README.md` with the new thin-pointer version. Use the Write tool. The new content:

    ```markdown
    # APC_64_40_11

    Remote script for the Akai APC40 with Ableton Live 11 (and seems to be working with 12) based on APC_40_9 Remote script. Bumped the remote script version to Ableton Live 11 by converting the whole codestack from Python 2 to 3.

    ![IMAGE](https://cdn.mos.cms.futurecdn.net/70f75473c4327722ce7a9e6d7f31b82d-1200-80.jpg)

    ## Documentation

    - [Install & first-run setup](docs/INSTALL.md)
    - [Interactive controller manual](docs/manual.html) — open in any browser
    - [Troubleshooting & known limitations](docs/TROUBLESHOOTING.md)

    Printable layout poster: [docs/apc40-layout.svg](docs/apc40-layout.svg)

    ## Credits

    APC 64-40 script Originally developed by [Hanz Petrov](http://remotescripts.blogspot.com/p/apc-64-40.html). Updated by [Fabrizio Poce](http://www.fabriziopoce.com/download.html) for Live 9. Final update released [on the Ableton forums](https://forum.ableton.com/viewtopic.php?f=1&t=204713) and lastly combined by [matthewcieplak](https://github.com/matthewcieplak/APC_64_40_9) and made the remote script compatible with Ableton 9.x.x.

    Uses parts from [Will Marshall's APCAdvanced script](https://github.com/willrjmarshall/AbletonDJTemplateUnsupported) for the VU metering.
    ```

    Specifically what is dropped from the original README:
    - The "Below you can find the original README..." sentence (line 6 of current).
    - The full Installation paragraph block (lines 7-20 of current) — moved to docs/INSTALL.md by Plan 07.
    - The "Instructions" section (lines 22-30 of current) — the broken `remotescripts.blogspot.com` instructional links and the `APC_64_40_quickstart_guide_rev_1.pdf` reference are explicitly dropped per D-02. The YouTube link is also dropped.
    - The "Enjoy!!!!" line.

    What is preserved verbatim:
    - The project blurb (current line 3): `Remote script for the Akai APC40 with Ableton Live 11 (and seems to be working with 12) based on APC_40_9 Remote script. Bumped the remote script version to Ableton Live 11 by converting the whole codestack from Python 2 to 3.`
    - The image (current line 4): `![IMAGE](https://cdn.mos.cms.futurecdn.net/70f75473c4327722ce7a9e6d7f31b82d-1200-80.jpg)`
    - The Credits content (current lines 32-37) — folded under a `## Credits` heading.

    What is added:
    - A new `## Documentation` section with three Markdown links (INSTALL.md, manual.html, TROUBLESHOOTING.md) and one auxiliary link (apc40-layout.svg).

    Final line count check: the rewritten file must be ≤ 25 lines (markdown source lines, including blanks). Verify with `wc -l README.md`.

    Terminology gate: `grep -ci 'snapshot' README.md` must return 0.

    **Link-target sanity check (per I-05 split):** before declaring the task done, verify all four link targets exist on disk:
    - `test -f docs/INSTALL.md` — created by Plan 07 (wave 2)
    - `test -f docs/manual.html` — created by Plan 01 (wave 1, Plan 08 depends on Plan 01)
    - `test -f docs/TROUBLESHOOTING.md` — created by Plan 07 (wave 2)
    - `test -f docs/apc40-layout.svg` — created by Plan 06 (wave 2, Plan 08 depends on Plan 06)

    If ANY of these targets is missing when this task runs, that means the wave ordering violated expectations — flag a deviation. The README write itself does not block on the targets (it's just markdown text), but a broken link in a freshly-shipped README would be embarrassing.
  </action>
  <verify>
    <automated>test -f README.md && grep -q '\[Install' README.md && grep -q 'docs/INSTALL.md' README.md && grep -q 'docs/manual.html' README.md && grep -q 'docs/TROUBLESHOOTING.md' README.md && grep -q 'docs/apc40-layout.svg' README.md && grep -q '## Credits' README.md && grep -q 'Hanz Petrov' README.md && grep -q 'Will Marshall' README.md && [ "$(wc -l < README.md)" -le 25 ] && [ "$(grep -ci 'snapshot' README.md)" = "0" ] && ! grep -q 'remotescripts.blogspot.com/p/apc-64-40.html.*Mappings\|APC_64_40_quickstart_guide_rev_1.pdf' README.md && test -f docs/INSTALL.md && test -f docs/manual.html && test -f docs/TROUBLESHOOTING.md && test -f docs/apc40-layout.svg</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` is at most 25 lines long (`wc -l README.md` ≤ 25).
    - Contains a Markdown link to `docs/INSTALL.md`.
    - Contains a Markdown link to `docs/manual.html`.
    - Contains a Markdown link to `docs/TROUBLESHOOTING.md`.
    - Contains a Markdown link to `docs/apc40-layout.svg`.
    - Contains the `## Credits` heading.
    - Preserves the Hanz Petrov / Fabrizio Poce / matthewcieplak / Will Marshall credits verbatim.
    - Drops the `APC_64_40_quickstart_guide_rev_1.pdf` link (no occurrence in the file).
    - Drops the remotescripts.blogspot.com Mappings/StepSequencer instructional links (the bare blogspot URL inside the Hanz Petrov credit may remain because it's part of his bio link).
    - Drops the YouTube features-video link.
    - Case-insensitive count of `snapshot` is 0.
    - The original opening project blurb sentence is preserved.
    - All four link targets exist on disk at task completion (`test -f docs/INSTALL.md`, `test -f docs/manual.html`, `test -f docs/TROUBLESHOOTING.md`, `test -f docs/apc40-layout.svg`) — the verify command checks all four.
  </acceptance_criteria>
  <done>
    `README.md` is now ≤ 25 lines: project blurb + image + a Documentation section with 3 doc links + the SVG poster link + the Credits section. No PDF reference, no broken instructional blogspot links, no YouTube link. The Credits content matches the original verbatim. No snapshot terminology. All four link targets exist on disk.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader → README.md | Read-only static prose. No code execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | N/A | README.md | accept | Documentation-only — same justification as the other plans. |
</threat_model>

<verification>
Manual review pass:
1. Open `README.md` — confirm it's a quick-glance landing page (≤ 25 lines), the four doc links work (relative paths from repo root), and the Credits section is intact.
2. Run `grep -ci snapshot README.md` — must return 0.
3. Run the four `test -f` commands to confirm link targets exist.
</verification>

<success_criteria>
DOC-02 (controller layout reference — README links to it) is partially expressed via README's link to `docs/manual.html` and `docs/apc40-layout.svg`. README.md is the thin entry pointer per D-02. Terminology gate (no `snapshot` in README) passes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-create-user-manual/06-08-SUMMARY.md` documenting:
- The line count of the rewritten README.md (must be ≤ 25).
- Confirmation that all four link targets exist on disk.
- Confirmation that the snapshot=0 terminology gate passes.
- Final note: phase 6 deliverables complete; ready for phase verification.
</output>
</task>
