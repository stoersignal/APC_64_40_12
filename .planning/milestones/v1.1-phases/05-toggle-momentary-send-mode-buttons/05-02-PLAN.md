---
phase: 05-toggle-momentary-send-mode-buttons
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - tests/test_05_mint03_isolation.py
autonomous: true
requirements: [MINT-03]

must_haves:
  truths:
    - "Send mode toggle/momentary operates without interfering with Solo/Mute toggle/momentary"
    - "Two simultaneous ToggleMomentaryChannelStrip instances maintain independent state during Send mode switches"
    - "Holding a Send mode button does not alter _solo_momentary_active or _mute_momentary_active on any track"
  artifacts:
    - path: "tests/test_05_mint03_isolation.py"
      provides: "MINT-03 isolation verification: Send mode changes don't bleed into channel strip state"
      exports: ["TestMint03Isolation"]
  key_links:
    - from: "EncModeSelectorComponent._mode_value"
      to: "ToggleMomentaryChannelStripComponent._solo_momentary_active"
      via: "no shared state — isolation test confirms they are independent"
      pattern: "_send_momentary_active.*_solo_momentary_active"
---

<objective>
Verify MINT-03: Send mode toggle/momentary behavior operates independently from Solo/Mute toggle/momentary behavior.

Purpose: Prove there is no shared state or callback cross-contamination between EncModeSelectorComponent's Send mode state machine and ToggleMomentaryChannelStripComponent's per-track solo/mute state machines. Both operate independently.
Output: A passing test file that demonstrates isolation under concurrent operation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-toggle-momentary-send-mode-buttons/05-CONTEXT.md
@.planning/phases/05-toggle-momentary-send-mode-buttons/05-01-SUMMARY.md

@EncModeSelectorComponent.py
@ToggleMomentaryChannelStripComponent.py
@tests/framework_stubs.py
</context>

<interfaces>
<!-- Key contracts from plan 05-01 output -->

From EncModeSelectorComponent.py (post-05-01):
```python
# New state variables added in 05-01:
self._send_ticks_delay = -1
self._send_momentary_active = False

# LONG_PRESS_DELAY = 4 defined at module level
```

From ToggleMomentaryChannelStripComponent.py:
```python
# Per-instance state — no shared class variables:
self._solo_ticks_delay = -1
self._solo_momentary_active = False
self._mute_ticks_delay = -1
self._mute_momentary_active = False
```

From tests/framework_stubs.py:
```python
class TrackStub:
    def __init__(self, solo=False, mute=False): ...

class SpecialChanStripStub:
    """Used as base for ToggleMomentaryChannelStripComponent in tests."""
    def __init__(self): ...
    def is_enabled(self): ...
    def song(self): ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MINT-03 isolation test — Send mode changes don't bleed into strip state</name>
  <files>tests/test_05_mint03_isolation.py</files>

  <read_first>
    - tests/framework_stubs.py (full file — all stubs available)
    - tests/test_task3_multi_track.py (multi-strip test pattern from v1.0 — understand how two strips are isolated)
    - tests/test_05_send_mode_toggle_momentary.py (stub injection pattern from 05-01)
    - EncModeSelectorComponent.py (post-05-01 — verify _send_momentary_active is the only new cross-cutting variable)
    - ToggleMomentaryChannelStripComponent.py (confirm no shared class-level state)
  </read_first>

  <behavior>
    Test cases for MINT-03 isolation:

    TC-MINT-01: Long press Send A does not mutate _solo_momentary_active on any channel strip.
      - Create EncModeSelectorComponent with stubs (reuse make_component() pattern from test_05_send_mode_toggle_momentary.py)
      - Create two ToggleMomentaryChannelStripComponent instances (strip_1, strip_2) each with a TrackStub (solo=False)
      - Set strip_1._track.solo = True (simulate strip in active solo state)
      - Simulate long press on Send A button (press-down, tick LONG_PRESS_DELAY+1 times)
      - Assert: enc._send_momentary_active == True
      - Assert: strip_1._solo_momentary_active == False (unchanged)
      - Assert: strip_2._solo_momentary_active == False (unchanged)
      - Assert: strip_1._track.solo == True (unchanged — Send mode press did not touch track state)

    TC-MINT-02: Solo long-press on strip does not mutate _send_momentary_active.
      - Create EncModeSelectorComponent with Send A active (mode 1)
      - Create strip_1 with TrackStub(solo=False)
      - Simulate long solo press on strip_1 (value=127, tick LONG_PRESS_DELAY+1 times)
      - Assert: strip_1._solo_momentary_active == True
      - Assert: enc._send_momentary_active == False (Send mode state untouched)
      - Assert: enc._mode_index == 1 (mode unchanged)

    TC-MINT-03: Simultaneous long holds — both state machines progress independently.
      - Create enc + strip_1 with a track
      - Simulate: press Send B button on enc AND start solo press on strip_1 in same "tick sequence"
        (call _mode_value for enc, call _solo_value for strip_1, then tick both _on_timer independently)
      - After LONG_PRESS_DELAY+1 ticks of both:
        - Assert enc._send_momentary_active == True
        - Assert strip_1._solo_momentary_active == True
      - Release both:
        - enc: simulate release → mode should revert to 0
        - strip: simulate release → track.solo should revert to original value
      - Assert: enc._mode_index == 0, strip_1._track.solo == False

    TC-MINT-04: Two channel strips are independent (mirrors v1.0 MULTI-01/02).
      - Create strip_1 and strip_2 each with separate TrackStub instances
      - Long press solo on strip_1 → assert only strip_1._solo_momentary_active == True
      - strip_2._solo_momentary_active must remain False throughout

    Note: ToggleMomentaryChannelStripComponent is injected via the same sys.modules stub pattern. The stubs from test_05_send_mode_toggle_momentary.py can be imported or replicated — prefer a lightweight local inline stub to keep test self-contained.
  </behavior>

  <action>
    1. Read tests/test_task3_multi_track.py to understand the multi-strip isolation pattern from v1.0.

    2. Read tests/test_05_send_mode_toggle_momentary.py to understand the enc stub setup from 05-01.

    3. Create tests/test_05_mint03_isolation.py:
       a. Reuse the same sys.modules injection block from test_05_send_mode_toggle_momentary.py at the top (same stubs needed for EncModeSelectorComponent).
       b. Add SpecialChanStripStub from framework_stubs.py into sys.modules as `_Framework.ChannelStripComponent.ChannelStripComponent` and a minimal SpecialChanStripComponent stub so ToggleMomentaryChannelStripComponent can be imported.
       c. For ToggleMomentaryChannelStripComponent, inject a SpecialChanStripStub (from framework_stubs.py) as the base class by patching sys.modules BEFORE importing ToggleMomentaryChannelStripComponent.
       d. Implement make_enc() and make_strip(track=None) helpers.
       e. Implement tick_enc(enc, n) and tick_strip(strip, n) helpers.
       f. Four test methods per TC-MINT-01 through TC-MINT-04.

    4. Run: `cd /Users/stoersignal/Dev/APC_64_40_12 && python -m pytest tests/test_05_mint03_isolation.py -v`
       All 4 tests must pass on first run (no RED phase needed — this is verification, not new logic).

    5. Run full suite: `python -m pytest tests/ -v 2>&1 | tail -20`
       All tests pass.

    6. Commit: `git commit -m "test(05-02): add MINT-03 isolation tests — Send mode and Solo/Mute are independent"`
  </action>

  <verify>
    <automated>cd /Users/stoersignal/Dev/APC_64_40_12 && python -m pytest tests/test_05_mint03_isolation.py tests/test_05_send_mode_toggle_momentary.py -v 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - tests/test_05_mint03_isolation.py exists
    - `python -m pytest tests/test_05_mint03_isolation.py -v` shows 4 passed, 0 failed
    - `python -m pytest tests/ -v` shows all tests pass (no regressions)
    - grep "TC-MINT-0" tests/test_05_mint03_isolation.py returns 4 matches (TC-MINT-01 through TC-MINT-04 referenced in comments)
  </acceptance_criteria>

  <done>4 MINT-03 isolation tests pass, confirming Send mode and Solo/Mute state machines are fully independent. Full suite green.</done>
</task>

</tasks>

<verification>
Final verification across both Phase 5 plans:
- `python -m pytest tests/ -v` → all tests pass including test_05_* files
- grep "_send_ticks_delay" EncModeSelectorComponent.py returns 4+ matches
- grep "LONG_PRESS_DELAY" EncModeSelectorComponent.py returns 1 match (defined locally)
- grep "_send_momentary_active" EncModeSelectorComponent.py returns 4+ matches
- No stale encoder bindings: EncModeSelectorComponent.update() non-Pan branch still calls pan16.set_parameter_controls(None)
</verification>

<success_criteria>
MINT-03 satisfied: Send A/B/C toggle/momentary operates independently from Solo/Mute toggle/momentary. No shared state. Four isolation tests pass. Full test suite green with no regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-toggle-momentary-send-mode-buttons/05-02-SUMMARY.md`
</output>
